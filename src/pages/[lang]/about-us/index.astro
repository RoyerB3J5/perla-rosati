---
import en from "../../../i18n/en";
import es from "../../../i18n/es";
import Layout from "../../../layouts/Layout.astro";
import Hero from "../../../sections/Main/Hero.astro";
import Perla from "../../../sections/Main/Perla.astro";
import Process from "../../../sections/Main/Process.astro";
import Questions from "../../../sections/Main/Questions.astro";
import Me from "../../../sections/Me.astro";
import Reviews from "../../../sections/Main/Reviews";
import type { Language } from "../../../types/language.type";

export function getStaticPaths() {
  return [{ params: { lang: "es" } }, { params: { lang: "en" } }];
}

export const defaultRedirect = "/en";

const lang = Astro.params.lang as Language;
const translations = { es, en };
const t = translations[lang];
const seo = t.seo.aboutUs;
---

<Layout
  lang={lang}
  t={t}
  title={seo.title}
  description={seo.description}
  keywords={seo.keywords}
  canonicalPath="/about-us"
>
  <Hero content={t.aboutUs.hero} changeColor={true} />
  <Perla content={t.aboutUs.perla} lang={lang} isButton={false} />
  <Reviews items={t.main.reviews} client:load />
  <Me content={t.aboutUs.me} />
  <Process content={t.aboutUs.process} lang={lang} />
  <Questions content={t.aboutUs.questions} lang={lang} />
</Layout>
